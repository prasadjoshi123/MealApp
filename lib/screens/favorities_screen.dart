import 'package:flutter/material.dart';

class FavortiesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('The Favorties !!'),
    );
  }
}